// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: "export",
  basePath: "/scheduleshare",
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
